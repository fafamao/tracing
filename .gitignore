# Ignore a specific directory and everything inside it
# (Common for build output, dependencies, etc.)
build/
bin/
node_modules/

# Ignore files with a specific extension
*.log
*.tmp
*.o

# Ignore a specific file
secret_credentials.txt

# You can also ignore files but make exceptions
# Ignore all .txt files...
*.txt
# ...except for this specific one
!important.txt